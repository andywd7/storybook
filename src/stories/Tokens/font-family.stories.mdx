import {
  Meta,
  Title,
  Subtitle,
  Story,
  Canvas,
  ArgsTable 
} from '@storybook/addon-docs/blocks'
import { Var } from "@dan-nz/storybook-documentation-primitives"
import docTokens from "../../tokens/docs"
import * as helpers from "../story-helpers"

export const tokensFontFamily = Object.values(docTokens.fontFamily).map((t, i) => {
  return (
    <tr key={i}>
      <td><Var>{"$" + t.name}</Var></td>
      <td style={{ fontFamily: t.value }}>{t.value}</td>
    </tr>
  )
})

<Meta title='TOKENS/Font family' parameters={{ previewTabs: { canvas: { hidden: true } } }} />

<Title />

<Subtitle>Tokens subtitle</Subtitle>

<table className="table">
  <thead className="thead-dark">
    <tr>
        <th>Token</th>
        <th>Value</th>
    </tr>
  </thead>
  <tbody>{tokensFontFamily}</tbody>
</table>
