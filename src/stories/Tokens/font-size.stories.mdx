import {
  Meta,
  Title,
  Subtitle,
  Story,
  Canvas,
  ArgsTable 
} from '@storybook/addon-docs/blocks'
import { Var } from "@dan-nz/storybook-documentation-primitives"
import docTokens from "../../tokens/docs"
import * as helpers from "../story-helpers"

export const tokensFontSize = Object.values(docTokens.fontSize).map((t, i) => {
  return (
    <tr key={i}>
      <td><Var>{"$" + t.name}</Var></td>
      <td>{helpers.rems(t.value)}</td>
      <td>{t.value}</td>
      <td><span style={{ fontSize: t.value }}>{t.name}</span></td>
    </tr>
  )
})

<Meta title='TOKENS/Font size' parameters={{ previewTabs: { canvas: { hidden: true } } }} />

<Title />

<Subtitle>Tokens subtitle</Subtitle>

<table className="table">
  <thead className="thead-dark">
    <tr>
        <th>Token</th>
        <th>rem</th>
        <th>px</th>
        <th>Example</th>
    </tr>
  </thead>
  <tbody>{tokensFontSize}</tbody>
</table>
