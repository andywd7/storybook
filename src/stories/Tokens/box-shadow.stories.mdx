import {
  Meta,
  Title,
  Subtitle,
  Story,
  Canvas,
  ArgsTable 
} from '@storybook/addon-docs/blocks'
import { Tile, Var } from "@dan-nz/storybook-documentation-primitives"
import docTokens from "../../../tokens-figma/generated/docs"
import { spacing08, white } from "../../../tokens-figma/generated/tokensAndPalette/tokensAndPalette"
import * as helpers from "../story-helpers"

export const tokensBoxShadow = Object.values(docTokens.boxShadow).map((t, i) => {
  return (
    <tr key={i}>
      <td><Var>{"$" + t.name}</Var></td>
      <td>{t.value}</td>
      <td><Tile value={helpers.removePx(spacing08)} color={white} square style={{ boxShadow: t.value }} /></td>
    </tr>
  )
})

<Meta title='TOKENS/Box shadow' parameters={{ previewTabs: { canvas: { hidden: true } } }} />

<Title />

<Subtitle>Tokens subtitle</Subtitle>

<table className="table">
  <thead className="thead-dark">
    <tr>
        <th>Token</th>
        <th>px</th>
        <th>Example</th>
    </tr>
  </thead>
  <tbody>{tokensBoxShadow}</tbody>
</table>
