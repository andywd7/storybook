import { Meta, Title, Primary, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Figma } from "storybook-addon-designs/blocks"
import { TabsState } from '@storybook/components'
import IiButton from './Button.vue'
import themeDark from "../../assets/themes/dark"
// import Nav from './Nav.mdx'
// import Nav from '../../Nav.js'
import Nav from './Nav.vue'
import Usage from './Usage.mdx'
import Code from './Code.mdx'
import Style from './Style.mdx'
import A11y from './A11y.mdx'
import { Var } from '@dan-nz/storybook-documentation-primitives';

import toReact from '@egoist/vue-to-react'

<!-- // export const sizeOptions = {
//   Default: undefined,
//   Small: "sm"
// }

// export const variantOptions = {
//   Null: null,
//   Primary: "primary",
//   Secondary: "secondary",
//   Standard: "standard",
//   Danger: "danger",
//   Ghost: "ghost"
// }

// export const typeOptions = {
//   Null: null,
//   Button: "button",
//   Submit: "submit",
//   Reset: "reset"
// } -->

export const FixedNav = toReact(Nav)

<!-- size: { control: { type: "select", options: sizeOptions } }, -->

<!-- argTypes={{
  variant: {
    control: {
      type: "select",
      options: variantOptions
    }
  },
  size: {
    control: {
      type: "select",
      options: sizeOptions
    }
  },
  type: {
    control: {
      type: "select",
      options: typeOptions
    }
  }
}} -->

<Meta
  title='Components/Button/Docs'
  component={IiButton}
  parameters={{
    layout: "centered"
  }}
/>

<Title />

export const val = Object.values(themeDark.theme)

export const tok = val.map((t, i) => {
  return <tr key={i}><td>${val[i].name}</td><td>{val[i].docs.role}</td><td><Var>{val[i].value}</Var></td></tr>
})

<!-- // export const tok = val.map((token, i) => {
//   console.log(token.value)
//   return `${token.value}`
// }) -->

<TabsState inititial="primaryDesign">
  <div id="primaryDesign" title="Design">
    <Figma url="https://www.figma.com/file/xV0XmrrCru3JAPH8w0FWb5/%F0%9F%93%90Netace-UI?node-id=83%3A2" />
  </div>
  <div id="primaryTokens" title="Tokens">
    <table width="100%">
      <thead>
        <tr>
          <th width="120">Token</th>
          <th>Role</th>
          <th width="120">Value</th>
        </tr>
      </thead>
      <tbody>{tok}</tbody>
    </table>
  </div>
</TabsState>

<!-- Object.entries(themeDark.theme).map((token, i) => {
  console.log(token[1])
  return (
    Object.entries(token[1]).map((value) => {
      console.log(value[1])
      return `${value[1]}`
    })
  )
}) -->

<!-- // export const Playground = `
//   <ii-button
//     :disabled="disabled"
//     :type="type"
//     :pressed="pressed"
//     :size="size"
//     :variant="variant">{{label}}</ii-button>
// ` -->

<!-- // export const Variant = `<ii-button :variant="variant">{{label}}</ii-button>` -->

<!-- // export const Template = (args, { argTypes }) => ({
//   props: Object.keys(argTypes),
//   components: { IiButton },
//   template: Playground
// }) -->

<!-- // export const variantTemplate = (args, { argTypes }) => ({
//   props: Object.keys(argTypes),
//   components: { IiButton },
//   template: Playground
// })

// ### Variants
// <Canvas>
//   <Story
//     name="Primary"
//     argTypes={{
//       variant: {
//         control: {
//           disable: true
//         }
//       }
//     }}
//     args={{
//       label: 'Primary',
//       variant: 'primary'
//     }}
//     parameters={{
//       docs: {
//         source: {
//           code: `<ii-button variant="primary">Button</ii-button>`
//         }
//       }
//     }}
//   >
//     {variantTemplate.bind()}
//   </Story>
// </Canvas> -->

<FixedNav />

<Code />

<!-- // export const Sizes = (args, { argTypes }) => ({
//   props: Object.keys(argTypes),
//   components: { IiButton },
//   template: `
// <div>
//   <ii-button :pressed="pressed" :type="type" :variant="variant">{{label}}</ii-button>
//   <ii-button :pressed="pressed" size="sm" :type="type" :variant="variant">{{label}}</ii-button>
// </div>
//   `
// }) -->

<!-- ## Sizes -->
<!-- {/* <Story
  name="Sizes"
  argTypes={{
    size: {
      control: {
        disable: true
      }
    }
  }}
  args={{ 
    label: 'Button'
  }}
  parameters={{
    docs: {
      source: {
        code: `
<ii-button>Button</ii-button>
<ii-button size="sm">Button</ii-button>`
      }
    }
  }}
>
  {Sizes.bind()}
</Story> */} -->

<!-- {/* <FixedNav />

<Usage />
<Code />
<Style />
<A11y /> */} -->
